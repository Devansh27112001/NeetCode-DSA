const solution_brute_force = (num1: string, num2: string): string => {
  const a = BigInt(num1) + BigInt(num2);
  return a.toString();
};

const solution_optimal = (num1: string, num2: string): string => {};
